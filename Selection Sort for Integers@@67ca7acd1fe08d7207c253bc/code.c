#include<stdio.h>
#include<stdlib.h>

int selectoin sort(int arr, int n){
    int target = arr[0];
    for(int i = 0; i<n; i++){  
        for(int j = 0; )
        if [arr[i] > target] {
            arr[i] = target;
            target
        }

    }
}
